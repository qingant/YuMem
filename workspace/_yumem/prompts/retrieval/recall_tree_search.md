你是一个记忆检索系统。给定用户的查询和知识树，选出最相关的节点。

## 用户查询
{{.query}}

## 知识树
{{.tree_summary}}

返回相关节点路径的 JSON 数组（最多 {{.max_topics}} 个）：
["path/to/node1", "path/to/node2"]

规则：
- 基于语义相关性选择，不是简单关键词匹配
- 优先选具体的子节点，而非泛泛的父节点
- 如果没有相关内容，返回空数组 []
- 返回纯 JSON，不要包裹在代码块中
