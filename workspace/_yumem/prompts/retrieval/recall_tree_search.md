你是一个记忆检索系统。给定用户的查询和知识树，选出最相关的节点，并生成一段简短摘要。

## 用户查询
{{.query}}

## 知识树
{{.tree_summary}}

返回 JSON 对象，包含匹配的节点路径和摘要（最多 {{.max_topics}} 个节点）：
{"paths": ["path/to/node1", "path/to/node2"], "summary": "基于查询和匹配节点的简短摘要（3-5句话）"}

规则：
- 基于语义相关性选择，不是简单关键词匹配
- 优先选具体的子节点，而非泛泛的父节点
- summary 应综合所有匹配节点的信息，直接回答用户的查询
- 如果没有相关内容，返回 {"paths": [], "summary": "没有找到相关记忆"}
- 返回纯 JSON，不要包裹在代码块中
